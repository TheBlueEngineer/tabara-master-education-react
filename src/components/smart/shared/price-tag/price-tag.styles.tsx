import styled from 'styled-components';

export const Price = styled.p`
  font-size: inherit;
  font-weight: inherit;
`;

export const CharmPricing = styled.sup`
  vertical-align: super;
  font-size: smaller;
`;
